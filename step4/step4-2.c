#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int N, X, i;
	scanf("%d %d", &N, &X);

	int A[N];
	for (i = 0; i < N; i++)
	{
		scanf("%d", &A[i])
	}

	for (i = 0; i < N; i++)
	{
		if (A[N] < X)
		{
			printf("%d", a[i]);
		}
	}

	return 0;
}